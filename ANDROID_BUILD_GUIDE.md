# 📱 دليل بناء وتحميل تطبيق أندرويد (APK) عبر السحابة (GitHub Actions)
# Android Cloud APK Build Guide

هذا الدليل يتيح لك بناء تطبيق الأندرويد **APK** تلقائياً عبر السحابة مجاناً بدون الحاجة إلى تثبيت Java أو Android SDK أو Android Studio على هاتفك أو حاسوبك الشخصي.

---

## 🚀 الطريقة الأولى: البناء بنقرة واحدة عبر GitHub Actions (موصى بها)

تم تجهيز ملف الأتمتة السحابية داخل المشروع تلقائياً في المسار:
`.github/workflows/build-apk.yml`

### الخطوات:

### 1️⃣ تصدير أو رفع المشروع إلى GitHub
- من القائمة الجانبية في **Google AI Studio**، اضغط على **Export to GitHub** أو نزّل المشروع كملف ZIP وارفعه إلى مستودع (Repository) خاص أو عام في حسابك على [GitHub](https://github.com).

### 2️⃣ تشغيل البناء السحابي (Run Workflow)
1. افتح مستودع المشروع على **GitHub** من متصفح الهاتف أو الكمبيوتر.
2. اضغط على تبويب **Actions** في الأعلى.
3. اختر من القائمة اليسرى سير العمل: **`Build Android APK`**.
4. اضغط على زر **`Run workflow`** الأزرق في اليمين:
   - يمكنك اختيار `debug` (لتثبيت مباشر للتجربة) أو `release`.
   - اضغط على **`Run workflow`** للبدء.

### 3️⃣ تحميل ملف الـ APK المباشر على هاتفك
1. انتظر دقيقتين إلى 3 دقائق حتى تكتمل عملية البناء (تظهر علامة صح خضراء `✔`).
2. اضغط على عملية البناء المكتملة.
3. في أسفل الصفحة تحت قسم **Artifacts**، ستجد ملف الـ APK جاهزاً للتحميل:
   - **`shuayb-recruitment-app-debug`**
4. اضغط عليه لتحميله مباشرة إلى هاتفك، ثم فك الضغط وثبّت ملف `app-debug.apk` مباشرة على هاتفك الأندرويد.

---

## 🛠️ ماذا تفعل الأتمتة السحابية (GitHub Actions) في الخلفية؟

1. **البيئة السحابية**: تفتح خادم Ubuntu سحابي قوي معزول ومجاني.
2. **الويب**: تثبت مكتبات Node.js وتبني تطبيق الويب بواسطة `vite build`.
3. **كاباسيتور (Capacitor)**: تقوم بمزامنة ملفات الويب مع مشروع الأندرويد (`npx cap sync android`).
4. **Android SDK & Java 21**: تقوم بتهيئة بيئة Android SDK الرسمية وOpenJDK 21.
5. **Gradle**: تنفذ أمر `./gradlew assembleDebug` لتجميع وتوليد ملف `app-debug.apk`.
6. **التسليم**: ترفع الـ APK النهائي كـ Artifact جاهز للتنزيل الفوري.

---

## 💡 نصائح عند التثبيت على الهاتف:
- عند فتح ملف الـ APK على هاتفك لأول مرة، قد يطلب منك النظام تفعيل خيار **"السماح بالتثبيت من هذا المصدر" (Allow from this source)** — فعّلها لمرة واحدة وسيتم تثبيت التطبيق بنجاح.

---

## 🌐 بدائل سحابية أخرى (Cloud CI/CD Alternatives)

إذا كنت تفضل منصات أخرى، يمكنك ربط مستودع GitHub مع:
- **Codemagic (codemagic.io)**: منصة متخصصة لتطبيقات الهواتف (Capacitor/Flutter/React Native) توفر بناء مجاني وتوقيع آلي للـ APK.
- **Bitrise (bitrise.io)**: سير عمل سحابي مرئي لتطبيقات الموبايل.
