name: Firebase Rules Tests

on:
  push:
    branches:
      - main
      - master
  pull_request:
    branches:
      - main
      - master
  workflow_dispatch:

permissions:
  contents: read

jobs:
  rules:
    name: 🔐 Firestore & Storage Rules
    runs-on: ubuntu-latest

    steps:
      - name: 📥 Checkout
        uses: actions/checkout@v4

      - name: 🟢 Setup Node.js 22.x
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: ☕ Setup Java 21
        uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: 21

      - name: 📦 Install project dependencies
        run: npm ci --legacy-peer-deps

      - name: 🧪 Install Firebase Rules test dependencies
        run: npm install --no-save @firebase/rules-unit-testing tsx

      - name: 🔥 Run Firebase Emulator Rules Tests
        run: |
          npx --yes firebase-tools@latest emulators:exec --only firestore,storage \
            "./node_modules/.bin/tsx scripts/firebase-rules.test.ts"
